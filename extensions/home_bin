if [ -d /usr/local/sbin ]; then
	PATH=/usr/local/sbin:$PATH
fi

if [ -d /usr/local/bin ]; then
	PATH=/usr/local/bin:$PATH
fi

HOMEBIN="$HOME/bin"
if [ -d $HOMEBIN ]; then
     PATH="$HOMEBIN:$PATH"
fi

